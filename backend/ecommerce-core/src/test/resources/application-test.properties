spring.main.allow-bean-definition-overriding=true

spring.datasource.url=jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1
spring.datasource.driverClassName=org.h2.Driver
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
spring.jpa.hibernate.ddl-auto=create-drop

# Testes não devem depender de migrations/infra real
spring.flyway.enabled=false

# Testes de controller não devem depender de segurança real
# spring.autoconfigure.exclude=org.springframework.boot.autoconfigure.security.servlet.SecurityAutoConfiguration

# Tokens dummy
MP_ACCESS_TOKEN=dummy_token
MP_WEBHOOK_SECRET=dummy_secret
JWT_SECRET=test_jwt_secret_only_for_tests
